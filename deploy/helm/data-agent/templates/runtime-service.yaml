{{- $c := .Values.components.runtime -}}
{{- if $c.enabled }}
{{- $cmpArgs := dict "root" $ "component" "runtime" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-agent.componentFullname" $cmpArgs }}
  labels:
    {{- include "data-agent.componentLabels" $cmpArgs | nindent 4 }}
  {{- with $c.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $c.service.type }}
  ports:
    - name: http
      port: {{ $c.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "data-agent.componentSelectorLabels" $cmpArgs | nindent 4 }}
{{- end }}

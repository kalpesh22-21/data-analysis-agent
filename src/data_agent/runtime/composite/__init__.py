"""Runtime composite tools — model-facing tools the runtime implements by
composing one or more MCP calls plus runtime-only logic (design §1).

Phase-1 first brick: `resolveValues` (D77) — see `resolve_values.py`.
"""
